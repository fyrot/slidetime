import type { PlasmoCSConfig } from "plasmo";
import "./content/slide-observer"


// for permissions
export const config: PlasmoCSConfig = {
  matches: ["https://docs.google.com/presentation/*"
  ]
};

// moved from slide-observer

